// use network::tcp::{test_main_tcp, ClientT, ServerT};

// fn main() {
//     println!("============");
//     let argv: Vec<String> = std::env::args().collect();
//     for (i, v) in argv.iter().enumerate() {
//         println!("i:{},v:{}", i, v);
//     }
//     let role = &argv[1];
//     let ip_port = "127.0.0.1:8789";
//     test_main_tcp(role, ip_port.to_string());
// }
